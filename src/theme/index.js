import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: "#0c4c5a",
        },
        secondary: {
            main: "#efa609",
        },
        accent: {
            main: "#53c2ac",
        },
        background: {
            default: "#fef5e1",
        },
    },
});

export default theme;